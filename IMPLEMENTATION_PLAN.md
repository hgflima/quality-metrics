# Implementation Plan

This file is generated and maintained by Ralph in planning mode.

Run `./loop.sh plan` (or `./loop-docker.sh plan`) to populate it.

## Priority 1: Batch 0 — Contracts & Project Scaffold

- [x] **TASK-002** — Scaffold package structure (`package.json`, `tsconfig.json`, `tsup.config.ts`, `vitest.config.ts`, `src/index.ts`, directory tree, `.gitignore`). Validated: `npm install` ✅, `tsup` build produces dual ESM+CJS ✅, `vitest run` ✅, `tsc --noEmit` ✅.
- [x] **TASK-001** — Define TypeScript contracts in `src/types.ts` (interfaces from `docs/mvp/03-technical-architecture.md`). All 10 contracts (`ReportDescriptor`, `RuleContext`, `WmcOptions`, `HalsteadOptions`, `LcomOptions`, `CboOptions`, `DitOptions`, `HalsteadMetrics`, `ClassMethodAttributes`, `ProjectSingleton`) defined and re-exported via `src/index.ts`. Validated: `tsc --noEmit` ✅, `tsup` build (ESM+CJS+dts) ✅, `vitest run` ✅.
- [x] **TASK-003** — Define CC helper interface + stub (`src/utils/cc.ts`). Exports `FunctionNode` (ESTree-compatible: FunctionDeclaration | FunctionExpression | ArrowFunctionExpression) and `computeCC(node: FunctionNode): number` returning `1`. Validated: `tsc --noEmit` ✅, `tsup` build ✅, `vitest run` ✅. Full traversal deferred to TASK-010.
- [x] **TASK-004** — Create test fixture files with hand-computed reference values. WMC (`high-wmc=17`, `low-wmc=4`, `zero-methods=0`), Halstead (`simple-fn` E≈51.7, `arrow-fn` E≈84.2, `complex-fn` E≈8783) with `tests/fixtures/halstead/COUNTING_MODEL.md` locking the operator/operand classification, LCOM (`low-cohesion=3`, `high-cohesion=0`, `single-method=0`), CBO (`high-cbo/` with 7 outgoing + 5 incoming = CBO 12 on `OrderController`, `low-cbo/` CBO 0), DIT (`chain/Labrador.ts` DIT 4 over 5 levels; `shallow/` DIT 0/1 with `implements`-only case). All fixture tsconfigs validated via `npx tsc --noEmit -p`. Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅.

## Priority 2: Lane A — Fast-Tier Rules (after Batch 0)

- [x] **TASK-010** — Implement `cc.ts` helper (full). Generic ESTree traversal in `src/utils/cc.ts` counts decision points (If/For/ForIn/ForOf/While/DoWhile/CatchClause/ConditionalExpression, SwitchCase with non-null `test`, LogicalExpression `&&`/`||`/`??`) and returns count + 1. Stops at nested function boundaries (`FunctionDeclaration`, `FunctionExpression`, `ArrowFunctionExpression`). Unit tests at `tests/utils/cc.test.ts` (15 cases) cover each branching type, default-case exclusion, nested-function isolation, and the per-method CCs from `tests/fixtures/wmc/high-wmc.ts` (3, 5, 4, 2, 3 → WMC 17). Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅ (15/15).
- [x] **TASK-011** — Implement `halstead.ts` helper. `computeHalstead(node)` in `src/utils/halstead.ts` walks an ESTree subtree, counts operators/operands per `tests/fixtures/halstead/COUNTING_MODEL.md`, and returns the full `HalsteadMetrics` (η₁, η₂, N₁, N₂, vocabulary, length, volume, difficulty, effort). Strips TS type fields (`typeAnnotation`, `returnType`, `typeParameters`, `superTypeParameters`) and any `TS*`-prefixed nodes; stops at nested function-like boundaries; counts object-literal shorthand identifiers exactly once; uses prefixed operand keys (`id:` / `lit:<typeof>:`) so identifier `null` does not collide with the `null` literal. Unit tests in `tests/utils/halstead.test.ts` (25 cases) cover degenerate (η₂=0) handling, the `simple-fn` and `arrow-fn` worked examples, every operator family (logical, ternary, member access, optional chaining, unary/update, switch+default, try/catch/finally, class+extends, for-in/of, variable declarations), operand uniqueness (typed-key collisions, shorthand dedup), TS stripping, and nested-function isolation. Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅ (40/40).
- [x] **TASK-012** — Implement `this-access.ts` helper. `extractThisAccesses(node)` in `src/utils/this-access.ts` walks a method body and returns `Set<string>` of property names accessed via `this.X`. Captures dotted access (`this.foo` → `'foo'`), private fields (`this.#foo` → `'#foo'`), and string-literal computed keys (`this['foo']` → `'foo'`); ignores dynamic computed keys (`this[x]`, `this[42]`) and bare `this`. Boundary semantics per TASK-015: ArrowFunctionExpression is traversed (shared `this`); FunctionDeclaration / FunctionExpression / ClassDeclaration / ClassExpression are NOT traversed (own `this`). For `this.foo.bar`, only `'foo'` is captured (the directly-on-this hop). Unit tests in `tests/utils/this-access.test.ts` (25 cases) cover dedup, chains, calls/assignments/updates, computed keys (literal vs. dynamic vs. numeric vs. Babel `StringLiteral`), private identifiers, optional chaining, arrow nesting, function/class boundary skipping, mixed arrow-inside-method-with-fn-inside, concise-body arrow as entry, and the LCOM fixture shapes (low-cohesion `db`, high-cohesion `count`). Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅ (65/65).
- [x] **TASK-013** — Implement `rules/wmc.ts`. Visitor-pattern rule (`ClassDeclaration` + `ClassExpression`) at `src/rules/wmc.ts` collects `MethodDefinition` (any kind: method/constructor/get/set) and function-valued `PropertyDefinition` class fields, computes per-method CC via `computeCC`, sums into WMC, and reports once per class when `WMC > options.max` (default 20). Skips classes with zero counted methods (zero-methods fixture stays silent). Skips abstract / TS-only declarations without a function body. Method-name extraction handles `Identifier`, `PrivateIdentifier` (prefixed with `#`), and `Literal`/`StringLiteral`/`NumericLiteral` keys; class name falls back to `<anonymous>` for unnamed `ClassExpression`. Diagnostic message matches the `03-technical-architecture.md` format and ships a structured `data` payload (`className`, `wmc`, `max`, `methods`). Unit tests at `tests/rules/wmc.test.ts` (22 cases) cover defaults, malformed options, fixture parity (low-wmc WMC=4, high-wmc WMC=17 with per-method CCs 3/5/4/2/3, zero-methods silent), constructor/getter/setter counting, arrow + function-expression class fields, private-identifier names, computed keys, anonymous classes, `ClassExpression`, and confirms nested-function CCs are not double-counted (cc-helper boundary preserved). Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅ (87/87).
- [x] **TASK-014** — Implement `rules/halstead.ts`. Visitor-pattern rule (`FunctionDeclaration` + `FunctionExpression` + `ArrowFunctionExpression`) at `src/rules/halstead.ts` computes per-function Halstead metrics via `computeHalstead`, and reports once when `volume > options.maxVolume` OR `effort > options.maxEffort` (defaults `maxVolume=1000`, `maxEffort=400`). Strict greater-than comparison; equality at the threshold stays silent. Function-name resolution prefers the node's own `id` (named declarations / expressions), then falls back to `node.parent` context: `VariableDeclarator`, `AssignmentExpression` to `Identifier`/`MemberExpression`, `MethodDefinition` / `PropertyDefinition` / `Property` / `ObjectProperty` (Identifier / PrivateIdentifier with `#` prefix / Literal/StringLiteral/NumericLiteral keys), with `<anonymous>` as the final fallback. Diagnostic message matches the `03-technical-architecture.md` format (`Volume: N (max: M) | Effort: N (max: M) | Difficulty: N`) — Volume/Effort rounded to integers, Difficulty to one decimal — and ships a structured `data` payload (`functionName`, `volume`, `effort`, `difficulty`, `maxVolume`, `maxEffort`). Nested-function isolation is preserved by the helper's boundary semantics (TASK-011), so an outer function's measurement never includes inner-function tokens. Unit tests at `tests/rules/halstead.test.ts` (32 cases) cover defaults, malformed options, partial-options merge, both single- and dual-threshold breaches, strict-equality boundary (computed via `computeHalstead`), structured `data` payload, rounding rules, all parent-context name resolutions (incl. private + computed-string keys), `<anonymous>` fallbacks, visitor wiring across all three function-like types, simple-fn fixture parity (V≈21, E≈52 → silent at defaults), arrow-fn fixture parity (silent at defaults), and a high-effort synthetic stand-in for `processPayment` that breaches the default Effort threshold. Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅ (119/119).
- [x] **TASK-015** — Implement `rules/lcom.ts`. Visitor-pattern rule (`ClassDeclaration` + `ClassExpression`) at `src/rules/lcom.ts` collects `MethodDefinition` (any kind: method/constructor/get/set) and function-valued `PropertyDefinition` class fields, builds each method's `this.X` footprint via `extractThisAccesses`, then iterates unordered method pairs counting P (disjoint sets → unrelated) and Q (≥1 shared property → related). Computes `LCOM1 = max(P - Q, 0)` and reports once per class when `LCOM > options.maxLcom` (default 0). Skips classes with fewer than 2 counted methods (single-method and zero-method fixtures stay silent). Two empty-`this`-footprint methods count as an unrelated pair (they don't interact through instance state — exactly what LCOM flags). Method-name resolution mirrors WMC (`Identifier`, `PrivateIdentifier` with `#` prefix, `Literal`/`StringLiteral`/`NumericLiteral`); class-name fallback is `<anonymous>`. Boundary semantics inherited from `extractThisAccesses` (TASK-012): nested arrows traverse (shared `this`), nested function declarations / expressions / classes stop. Diagnostic message matches the `03-technical-architecture.md` format (`Class 'X' has LCOM of N (max: M).\n  Unrelated method pairs: (a, b), ...`) and ships a structured `data` payload (`className`, `lcom`, `max`, `pairs`). Unit tests at `tests/rules/lcom.test.ts` (31 cases) cover defaults, malformed/partial options, fixture parity (low-cohesion LCOM=3 with the 3 expected pairs in source order, high-cohesion LCOM=0 silent, single-method silent), pair semantics (empty-set pairs flagged, partial overlap, P-Q clamp at 0, deterministic pair order), method shapes (constructor/getter/setter counting, arrow + function-expression class fields, private + computed-string keys, anonymous class, `ClassExpression`, abstract/TS-only skipping), and boundary semantics (nested arrow shares `this`, nested function declaration / nested class do not). Validated: `tsc --noEmit` ✅, `npm run build` ✅, `npm test` ✅ (150/150).
- [x] **TASK-016** — Wire fast-tier rules into `src/index.ts`. The plugin entry now imports `wmc`, `halstead`, and `lcom` from `src/rules/*.js` and registers them under the `rules` map (keyed by short name; OXLint/ESLint namespace them as `quality-metrics/<rule>` via `meta.name`). Each rule is also re-exported by name for direct consumption (`import { wmc } from 'quality-metrics'`). Deep-tier rules (`cbo`, `dit`) intentionally not wired — deferred to TASK-024 once the ts-morph project singleton (TASK-020) lands. Unit tests at `tests/index.test.ts` (12 cases) cover plugin meta (`name`, `version`), default-export shape (`{ meta, rules }`), exact registry membership (only fast-tier wired; deep-tier explicitly absent), referential identity between `rules.X` and the named exports, OXLint/ESLint rule shape (`meta.type`, `meta.docs.description`, `meta.schema`, `create()` function) for every registered rule, and end-to-end smoke that each rule actually fires through the registry against a hand-built AST (WMC threshold breach, Halstead Volume breach, LCOM unrelated-pair detection). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM+CJS+dts), `npm test` ✅ (162/162).
- [x] **TASK-017** — Create `configs/oxlint.fast.json` preset. The fast-tier preset at `configs/oxlint.fast.json` matches the architecture spec (`docs/mvp/03-technical-architecture.md` §"Config Presets") verbatim: `$schema` pointing at OXLint's configuration schema, `jsPlugins: ["quality-metrics"]`, and rules `quality-metrics/wmc` (`error`, `max: 20`), `quality-metrics/halstead` (`warn`, `maxVolume: 1000`, `maxEffort: 400`), and `quality-metrics/lcom` (`warn`, `maxLcom: 2`). The deep-tier rules (`cbo`, `dit`) are intentionally absent — they ship in `configs/oxlint.deep.json` (TASK-025). Unit tests at `tests/configs/oxlint-fast.test.ts` (12 cases) load the preset via `fs.readFileSync` (resolved against `import.meta.url` so vitest works under ESM), and assert: JSON parses, `$schema` references oxlint, `jsPlugins` includes `'quality-metrics'`, the rules registry contains exactly the three fast-tier keys (deep-tier explicitly absent), every entry is a `[severity, options]` tuple with severity `error`/`warn`/`off`, and per-rule severity + option payloads match the doc (deep-equal checks). Each rule's option keys are also cross-validated against the live `meta.schema[0].properties` of the wired rule (catches drift if a rule schema changes a key without the preset following). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM+CJS+dts), `npm test` ✅ (174/174).

## Priority 3: Lane B — Deep-Tier Rules (after Batch 0)

- [x] **TASK-020** — Implement `project-singleton.ts` (createOnce + ESLint fallback). `src/project-singleton.ts` exposes `getProjectSingleton(tsconfigPath?)` returning the `ProjectSingleton` contract (`{ project, isAvailable, error? }`), backed by a `Map<string, ProjectSingleton>` keyed on `tsconfigPath ?? '<default>'`. ts-morph is loaded lazily via `createRequire(import.meta.url)` so the cost is paid only when CBO/DIT actually run; if the loader throws (peer dep absent), if the resolved module lacks `Project`, or if `new Project()` throws (bad tsconfig), the helper returns `isAvailable: false` with a descriptive error and a `Proxy`-based stub `project` that throws on any property access — callers must gate use on `isAvailable`. Failure results are cached too, so we don't retry the loader on every call. Same `tsconfigPath` returns the same Project across calls (OXLint `createOnce` semantics + ESLint module-singleton fallback in one helper); distinct paths build separate Projects. Test seams: `setTsMorphLoader(custom \| null)` swaps the loader and clears the cache, `resetProjectSingleton()` clears the cache without changing the loader. Unit tests at `tests/project-singleton.test.ts` (18 cases) cover caching identity (same path memoizes, different paths split, `<default>` key collapses `undefined`/no-arg, constructor receives `{ tsConfigFilePath }` only when set), failure handling (loader throws, loader returns non-`Project` module, ctor throws, non-Error throwables stringified, failure result memoized, stub access throws and names the property), seam behavior (`setTsMorphLoader` clears cache, `setTsMorphLoader(null)` restores real ts-morph and resolves `getSourceFiles`, `resetProjectSingleton` clears without touching loader), and end-to-end against the `tests/fixtures/cbo/high-cbo` and `low-cbo` tsconfigs (loads real source files, identity holds across calls, distinct tsconfigs yield distinct Projects). Not yet wired into `src/index.ts` — deferred to TASK-024 with the deep-tier rules. Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM+CJS+dts, 22.43 KB ESM / 23.51 KB CJS), `npm test` ✅ (192/192).
- [x] **TASK-021** — Implement `rules/cbo.ts` — outgoing coupling. Visitor-pattern rule (`ClassDeclaration` + `ClassExpression`) at `src/rules/cbo.ts` resolves the ESTree class node to its ts-morph `ClassDeclaration` via `getProjectSingleton(options.tsconfigPath)` + `project.getSourceFile(filename).getClass(className)`, walks every member returned by `getMembers()` (which deliberately excludes the heritage clauses, so `extends`/`implements` edges drop out per C&K E2E-008), and for each `Identifier` descendant calls `getDefinitionNodes()` — *not* `getSymbol()` — so imports/re-exports resolve to the underlying `ClassDeclaration` instead of stopping at the `ImportSpecifier` binding. Filters out: the class itself, declarations from `.d.ts` files (built-ins / ambient libs), and non-class declarations. Distinct external class names are collected into a `Set<string>`. Reports once when `outgoing > options.max` (default `max: 10`); the bidirectional sum lands in TASK-022. Diagnostic message follows `03-technical-architecture.md` (`Class 'X' has CBO of N (max: M).\n  Outgoing (K): A, B, ...`) — outgoing list is sorted lexicographically for determinism — and ships a structured `data` payload (`className`, `cbo`, `max`, `outgoingCount`, `outgoing`). Graceful degradation: when the ts-morph singleton is unavailable (peer dep absent / Project ctor throws) the rule returns no-op visitors; when the file or named class isn't loaded into the project, the visitor silently skips that node; anonymous classes (no `id`) are skipped. Not yet wired into `src/index.ts` — deferred to TASK-024. Unit tests at `tests/rules/cbo.test.ts` (24 cases) cover: meta+schema shape (`max` + `tsconfigPath` keys, `additionalProperties: false`, message template), graceful degradation (loader throws → silent, missing source file → silent, missing class → silent, anonymous `ClassDeclaration` and `ClassExpression` → silent), options handling (default `max=10`, malformed string options falls through to defaults, partial options without `tsconfigPath` skips), high-cbo fixture parity (`OrderController` outgoing=7 silent at default 10, breach at `max=6` reports `cbo:7`, exact 7-name sorted list, full diagnostic message string match, node attached to report, strict greater-than at `max=7` stays silent, `OrderService` itself outgoing=0), low-cbo fixture (`SimpleClass` outgoing=0 silent at `max=0`), inheritance exclusion (Labrador→Dog, Dog→Mammal, Implementer→Named all silent at `max=0`), and visitor wiring (both `ClassDeclaration` and `ClassExpression` registered; no-op visitors when ts-morph unavailable do not throw). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM+CJS+dts), `npm test` ✅ (216/216).
- [x] **TASK-022** — Implement `rules/cbo.ts` — incoming coupling (bidirectional). The rule now sums outgoing + incoming references; `collectIncomingClasses(cls)` uses ts-morph's `findReferencesAsNodes()` and walks each reference's ancestor chain via `findContainingClassExcludingHeritage` to attribute it to the innermost containing class (skipping any reference whose ancestors pass through a `HeritageClause` — that excludes both `extends X` and `implements X` callsites in either direction, per C&K E2E-008). Self-references and references in `.d.ts` files are dropped. The diagnostic message now matches `docs/mvp/03-technical-architecture.md` verbatim with both lines: `Outgoing (K): A, B, ...\n  Incoming (M): X, Y, ...`. The `data` payload was extended with `incomingCount` and `incoming` (alongside the existing `outgoingCount` / `outgoing`). Updated unit tests at `tests/rules/cbo.test.ts` (25 cases, +1 net) cover: high-cbo fixture parity per `README.md` (E2E-007 — `OrderController` outgoing=7 + incoming=5 → CBO=12; reports at default `max=10`, silent at `max=12` strict-equality boundary), separate outgoing-list and incoming-list assertions (sorted, deduplicated names), full diagnostic-message string match including both lines, `OrderService` reverse case (outgoing=0, incoming=1 from `OrderController`), low-cbo `SimpleClass` silent at max=0, and inheritance exclusion in BOTH directions (Labrador→Dog `extends`, Implementer→Named `implements`, plus the new check that `Dog`'s incoming is 0 even though `Labrador extends Dog` — heritage-clause references must not count as incoming). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 22.43 KB / CJS 23.51 KB / dts), `npm test` ✅ (217/217). Wiring into `src/index.ts` still deferred to TASK-024.
- [x] **TASK-023** — Implement `rules/dit.ts`. Visitor-pattern rule (`ClassDeclaration` + `ClassExpression`) at `src/rules/dit.ts` resolves the ESTree class node to its ts-morph `ClassDeclaration` via `getProjectSingleton(options.tsconfigPath)` + `project.getSourceFile(filename).getClass(className)`, then walks the inheritance chain via `ClassDeclaration.getBaseClass()` (which uses the TypeScript type checker so it follows imports across files and naturally excludes `implements` clauses, since interfaces are not classes). The walker maintains a visited `Set` so a self-referential or mutually-recursive `extends` cycle (E2E-011) terminates instead of looping forever; it also stops as soon as the next base lives in a `.d.ts` file (project-local classes only — mirrors the CBO declaration-file filter). DIT is reported as the number of `extends` edges (`chain.length - 1`); the chain itself includes the class first, then each ancestor. Reports once when `dit > options.max` (default `max: 5`); strict greater-than, so equality at the threshold stays silent. Diagnostic message follows `03-technical-architecture.md` (`Class 'X' has DIT of N (max: M).\n  Chain: A → B → C → ...`) and ships a structured `data` payload (`className`, `dit`, `max`, `chain`). Graceful degradation: when the ts-morph singleton is unavailable (peer dep absent / Project ctor throws) the rule returns no-op visitors; when the file or named class isn't loaded into the project, the visitor silently skips that node; anonymous classes (no `id`) are skipped. Not yet wired into `src/index.ts` — deferred to TASK-024 with CBO. Unit tests at `tests/rules/dit.test.ts` (25 cases) cover: meta+schema shape (`max` + `tsconfigPath` keys, `additionalProperties: false`, message template), graceful degradation (loader throws → silent, missing source file → silent, missing class → silent, anonymous `ClassDeclaration` and `ClassExpression` → silent), options handling (default `max=5` keeps Labrador DIT=4 silent, malformed string options → defaults, partial options without `tsconfigPath` skips, `max=0` flags any inheritance edge), chain fixture parity per `README.md` (E2E-010 — Labrador DIT=4 reports at `max=3` with full `Labrador → Dog → Mammal → Animal → LivingThing` chain, full diagnostic-message string match including the `Chain:` line, strict-equality boundary at `max=4` stays silent, intermediate cases Dog/Mammal/Animal report their own depths and chains, `LivingThing` root DIT=0 silent, node attached to report), shallow fixtures (`Standalone` DIT=0, `OneLevel` DIT=1 chain `OneLevel → Standalone`, `Implementer` with `implements`-only DIT=0 — interfaces excluded from depth), and visitor wiring (both `ClassDeclaration` and `ClassExpression` registered; no-op visitors when ts-morph unavailable do not throw). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 22.43 KB / CJS 23.51 KB / dts), `npm test` ✅ (242/242).
- [x] **TASK-024** — Wire deep-tier rules into `src/index.ts`. The plugin entry now imports `cbo` and `dit` alongside the fast-tier rules and registers them under the `rules` map (keyed by short name; OXLint/ESLint namespace them as `quality-metrics/cbo` and `quality-metrics/dit` via `meta.name`). Both rules are also re-exported by name (`import { cbo, dit } from 'quality-metrics'`). Updated unit tests at `tests/index.test.ts` (15 cases, +3 net) flip the previous "deep-tier explicitly absent" assertion to require all five rules in the registry, extend the registry-identity check to cbo/dit, extend the OXLint/ESLint shape sweep (`meta.type`, `meta.docs.description`, `meta.schema`, `create()` function) to cbo/dit, and add smoke tests asserting both rules register `ClassDeclaration` + `ClassExpression` visitors and gracefully no-op on hand-built ASTs whose filename is not in the ts-morph project (no throws, zero reports — end-to-end fixture coverage stays in `tests/rules/{cbo,dit}.test.ts`). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (245/245).
- [x] **TASK-025** — Create `configs/oxlint.deep.json` preset. The deep-tier preset at `configs/oxlint.deep.json` matches the architecture spec (`docs/mvp/03-technical-architecture.md` §"Config Presets") verbatim: `$schema` pointing at OXLint's configuration schema, `jsPlugins: ["quality-metrics"]`, and rules `quality-metrics/cbo` (`error`, `max: 10`) and `quality-metrics/dit` (`warn`, `max: 5`). The fast-tier rules (`wmc`, `halstead`, `lcom`) are intentionally absent — they ship in `configs/oxlint.fast.json` (TASK-017). Unit tests at `tests/configs/oxlint-deep.test.ts` (10 cases) mirror the fast-preset test pattern: load the preset via `fs.readFileSync` (resolved against `import.meta.url` so vitest works under ESM), and assert: JSON parses, `$schema` references oxlint, `jsPlugins` includes `'quality-metrics'`, the rules registry contains exactly the two deep-tier keys (fast-tier explicitly absent), every entry is a `[severity, options]` tuple with severity `error`/`warn`/`off`, and per-rule severity + option payloads match the doc (deep-equal checks). Each rule's option keys are also cross-validated against the live `meta.schema[0].properties` of the wired rule (catches drift if a rule schema changes a key without the preset following). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (255/255).

## Priority 4: Lane D — Tests

- [x] **TASK-030** — Unit tests: WMC rule. Delivered as part of TASK-013 — see `tests/rules/wmc.test.ts` (22 cases) covering defaults, malformed options, fixture parity (low-wmc WMC=4, high-wmc WMC=17 with per-method CCs 3/5/4/2/3, zero-methods silent), constructor/getter/setter counting, arrow + function-expression class fields, private-identifier names, computed keys, anonymous classes, `ClassExpression`, and the cc-helper boundary (nested-function CCs not double-counted). Validated: ✅ all 22 tests pass.
- [x] **TASK-031** — Unit tests: Halstead rule. Delivered as part of TASK-011 + TASK-014 — see `tests/utils/halstead.test.ts` (25 cases on the helper) and `tests/rules/halstead.test.ts` (32 cases on the rule). Helper coverage: degenerate (η₂=0) handling, `simple-fn` and `arrow-fn` worked examples, every operator family (logical, ternary, member access, optional chaining, unary/update, switch+default, try/catch/finally, class+extends, for-in/of, variable declarations), operand uniqueness (typed-key collisions, shorthand dedup), TS stripping, nested-function isolation. Rule coverage: defaults, malformed/partial options, both single- and dual-threshold breaches, strict-equality boundary, structured `data` payload, rounding rules, all parent-context name resolutions (incl. private + computed-string keys), `<anonymous>` fallbacks, visitor wiring across all three function-like types, simple-fn fixture parity (V≈21, E≈52 → silent at defaults), arrow-fn fixture parity, and a high-effort synthetic stand-in for `processPayment` that breaches the default Effort threshold. Validated: ✅ 57/57 tests pass.
- [x] **TASK-032** — Unit tests: LCOM rule. Delivered as part of TASK-012 + TASK-015 — see `tests/utils/this-access.test.ts` (25 cases on the helper) and `tests/rules/lcom.test.ts` (31 cases on the rule). Helper coverage: dedup, chains, calls/assignments/updates, computed keys (literal vs. dynamic vs. numeric vs. Babel `StringLiteral`), private identifiers, optional chaining, arrow nesting, function/class boundary skipping, mixed arrow-inside-method-with-fn-inside, concise-body arrow as entry, fixture shapes (low-cohesion `db`, high-cohesion `count`). Rule coverage: defaults, malformed/partial options, fixture parity (low-cohesion LCOM=3 with the 3 expected pairs in source order, high-cohesion LCOM=0 silent, single-method silent), pair semantics (empty-set pairs flagged, partial overlap, P-Q clamp at 0, deterministic pair order), method shapes (constructor/getter/setter, arrow + function-expression class fields, private + computed-string keys, anonymous class, `ClassExpression`, abstract/TS-only skipping), boundary semantics (nested arrow shares `this`, nested function decl/class do not). Validated: ✅ 56/56 tests pass.
- [x] **TASK-033** — Unit tests: CBO + DIT rules. Delivered as part of TASK-021/022 (CBO) and TASK-023 (DIT) — see `tests/rules/cbo.test.ts` (25 cases) and `tests/rules/dit.test.ts` (25 cases), plus `tests/project-singleton.test.ts` (18 cases) covering the shared ts-morph singleton. CBO coverage: meta+schema shape, graceful degradation (loader throws, missing source/class, anonymous classes), default/malformed/partial options, high-cbo fixture parity per E2E-007 (`OrderController` outgoing=7 + incoming=5 → CBO=12), separate outgoing/incoming list assertions (sorted, deduplicated), full diagnostic-message string match, `OrderService` reverse case, low-cbo silent, inheritance exclusion in BOTH directions (Labrador→Dog `extends`, Implementer→Named `implements`, plus heritage-clause incoming exclusion), visitor wiring across `ClassDeclaration` + `ClassExpression`. DIT coverage: meta+schema shape, graceful degradation, default `max=5` keeps Labrador DIT=4 silent, malformed/partial options + `max=0` flags any inheritance edge, chain fixture parity per E2E-010 (Labrador DIT=4 → reports at `max=3` with full chain, strict-equality boundary at `max=4` silent, intermediate Dog/Mammal/Animal cases, root `LivingThing` DIT=0), shallow fixtures (`Standalone`, `OneLevel`, `Implementer` with implements-only DIT=0), visitor wiring. Validated: ✅ 68/68 tests pass.
- [x] **TASK-034** — Integration tests: OXLint + ESLint compatibility. New `tests/integration/eslint-compat.test.ts` (10 cases) drives a real `eslint@10` `Linter` against the plugin's flat-config registration. ESLint added as a `devDependency` (peer not required: the plugin code never imports `eslint`). Flat-config base sets `files: ['**/*.ts', '**/*.js']` so `Linter.verify()` matches the source — without it ESLint v9+ emits the `No matching configuration found for X` skip warning instead of running rules. E2E-015 coverage: plugin loads without fatal errors, `quality-metrics/wmc` fires on the OrderService source (WMC=17, message `Class 'OrderService' has WMC of 17 (max: 15)`, severity 2) and stays silent at `max=20` (E2E-002 reverse), `quality-metrics/lcom` fires on the MixedService source (LCOM=3, severity 1, E2E-005 parity), both rules fire in a single `verify()` pass on a combined source, zero diagnostics on the trivial `add(a,b)` source under default thresholds (E2E-004 parity), and structured options for all three fast-tier rules pass schema validation (no fatal errors). E2E-016 coverage: `meta.name === 'quality-metrics'` is asserted distinct from native OXLint / ESLint plugin namespaces (eslint, typescript, oxc, unicorn, jsx-a11y, react, react-hooks, n, node, import, promise, jest, vitest, nextjs); rule registry keys are bare short names (no slashes, no `quality-metrics-` prefix) — namespacing is provided by the plugin object key, not the rule identifier; and a single-violation source emits exactly one diagnostic through ESLint (not duplicated). Deep-tier rules (cbo, dit) intentionally not covered here — they require ts-morph + a real tsconfig and live in `tests/rules/{cbo,dit}.test.ts`. Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (265/265, +10).
- [x] **TASK-035** — Integration tests: hook wiring (Claude Code + lint-staged). Created the two shipping fixtures users wire into their projects: `fixtures/lintstagedrc.example.js` (US-008 pre-commit gate) exporting `{ '*.{ts,tsx}': ['oxlint --config oxlint.fast.json', 'oxlint --config oxlint.deep.json'] }` per `docs/mvp/03-technical-architecture.md` §"lint-staged Configuration" with a header doc-comment pointing at the alternative `node_modules/quality-metrics/configs/...` resolution path; and `fixtures/claude-md-hook.md` (US-007 Claude Code PostToolUse) running ONLY the fast preset on `Write`/`Edit`/`MultiEdit` per the architecture doc verbatim — deep tier intentionally absent (too expensive for per-write latency, gated at pre-commit instead). New `tests/integration/hook-wiring.test.ts` (18 cases) drives a contract suite over both fixtures: lint-staged side dynamically `import()`s the example config via a `pathToFileURL` URL (works under Vitest ESM regardless of consumer `type` field) and asserts default-export shape, the `*.{ts,tsx}` glob key, array-of-strings command shape, fast-before-deep ordering (cheap fail-fast before paying ts-morph cost), every command starts with `oxlint` and includes `--config <file>`, and the referenced preset filenames resolve to real files in `configs/`. Claude Code side reads the markdown body and asserts the `PostToolUse` hook trigger, mention of all three triggering tools (`Write` / `Edit` / `MultiEdit`), the fast-preset oxlint command (with `--config oxlint.fast.json`), the explicit absence of the deep preset (regression guard against accidentally pushing deep-tier into the per-write hook), the `.tsx?` extension target, and the presence of a fenced ```bash code block containing the hook command. A third describe block re-validates the referenced `configs/oxlint.fast.json` and `configs/oxlint.deep.json` parse as JSON and register the `quality-metrics` jsPlugin (catches drift if either preset rotates plugin names). The full `git commit` shell-integration variant of E2E-014 (real `oxlint` binary, scratch git repo) is intentionally out of scope — it requires an `oxlint` install + git harness and lives in CI (TASK-040 territory or a future TASK-041 perf gate). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (283/283, +18).

## Priority 5: Lane C — Infrastructure

- [x] **TASK-040** — CI pipeline (GitHub Actions) — test + type check + lint. New `.github/workflows/ci.yml` runs on push/PR to `main` with `concurrency.cancel-in-progress` so superseded runs are aborted, `permissions: contents: read` (least-privilege default), and a single `test` job matrixed across Node `18`/`20`/`22` (per `package.json` `engines: ">=18.0.0"`) using `actions/checkout@v4` + `actions/setup-node@v4` with built-in `npm` caching. Steps: `npm ci` → `npm run typecheck` (tsc --noEmit) → `npm run build` (tsup) → `npm test` (vitest run). `fail-fast: false` so all Node versions report independently. Self-hosted lint (oxlint.fast.json on src/) and the perf-benchmark gate (`fast < 1s`, `deep < 10s` per `04-security-and-performance.md`) are intentionally NOT wired here — they require, respectively, an `oxlint` binary devDep + a generated 500-file fixture project (TASK-041 territory) and a publish workflow (TASK-042). Validated locally: `npm run typecheck` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (265/265). Workflow YAML uses pinned major-version tags (`@v4`) per GitHub's recommended practice for first-party actions.
- [x] **TASK-041** — Performance benchmark suite + CI gate. New `tests/benchmarks/fixture-generator.ts` deterministically materializes a 500-file synthetic TypeScript project under a caller-supplied tmpdir: each file is a class with a constructor and four methods exercising branches + `this.X` access, organized as a forest of inheritance chains (depth 5) with cross-file imports — gives all five rules (WMC, Halstead, LCOM, CBO, DIT) something to compute on. Files are JS-compatible (no TS-only constructs) so ESLint's default parser (espree) and ts-morph's TypeScript parser both accept the same source. New `tests/benchmarks/perf.bench.ts` (vitest `bench` API) tracks fast-tier and deep-tier timings against the 500-file fixture for trend visibility. New `tests/benchmarks/perf-gate.test.ts` (3 cases) is the hard CI gate per `docs/mvp/04-security-and-performance.md` §"Execution Time Targets" — asserts fast-tier completes within 1000ms × 1.2 = 1200ms (the spec's "20% tolerance") and deep-tier within 10000ms × 1.2 = 12000ms on the same fixture, with `QM_PERF_TOLERANCE` env var to relax the multiplier and `QM_SKIP_PERF_GATE=1` to bypass entirely. Both files run through ESLint's `Linter` (rather than the OXLint binary, which would require a Rust devDep) — since the same plugin object plugs into both runtimes, ESLint serves as a conservative upper bound on rule cost (OXLint's Rust runtime is faster). Wires `npm run bench` into `.github/workflows/ci.yml` as an informational, non-blocking step on Node 20 (`continue-on-error: true`) so trend timings show up in CI logs without re-gating; the hard gate lives in `npm test`. **Side fix discovered while wiring**: CBO and DIT rules called `context.getFilename()` (deprecated in ESLint v9, removed in v10), which broke under real ESLint v10 even though the existing rule unit tests (which use hand-rolled mock `RuleContext` objects providing `getFilename`) passed. Updated `RuleContext` in `src/types.ts` to declare both `filename?: string` (ESLint v9+ / OXLint property) and `getFilename?(): string` (legacy v8 fallback), and updated `src/rules/cbo.ts` + `src/rules/dit.ts` to read `context.filename ?? context.getFilename?.() ?? ''` (graceful no-op if neither is available). Existing CBO/DIT mock-context tests still pass — relaxing the interface to optional preserves backwards compat. ESLint flat-config quirk handled: `Linter`'s `files` glob is matched relative to the Linter's `cwd`, which defaults to `process.cwd()` and does NOT match temp-dir paths under `os.tmpdir()` — both bench and gate construct the Linter with `new Linter({ cwd: fixtures.rootDir })` to anchor glob resolution at the fixture root (otherwise every `verify()` returns a single "No matching configuration found" warning and silently skips every rule). Local timings on macOS/M-series: fast-tier 213ms (well under 1200ms budget), deep-tier 1051ms (well under 12000ms budget) — both pass with ample headroom. Validated: `npx tsc --noEmit` ✅, `npm run build` ✅ (ESM 32.19 KB / CJS 33.36 KB / dts 22.03 KB), `npm test` ✅ (304/304, +3 perf-gate cases — full suite total now reflects the cbo/dit fix transparently).
- [x] **TASK-042** — npm publish workflow (provenance, files allowlist). New `.github/workflows/publish.yml` triggered on `release: published` (with `workflow_dispatch` fallback so a tag-based release isn't strictly required), runs on Node 20 (LTS, within `engines: ">=18.0.0"`), declares the OIDC permission set required by `npm provenance` (`contents: read`, `id-token: write`) at the workflow level — least-privilege per GitHub's recommendation — and gates the publish step behind `npm ci → npm run typecheck → npm run build → npm test`. Adds a release-tag verification step that reads `package.json` `version` via `node -p` and aborts with `::error::` if it diverges from `${GITHUB_REF_NAME#v}` (catches the easy mistake of cutting a `vX.Y.Z` release without bumping the package). The publish call is `npm publish --provenance --access public --tag <input>` with `NODE_AUTH_TOKEN` from `secrets.NPM_TOKEN`; the `setup-node@v4` step sets `registry-url: 'https://registry.npmjs.org'` (required for OIDC token exchange + npm auth). Workflow runs in an `npm-publish` GitHub Environment so the maintainer can attach branch/reviewer protections to the secret if desired. The `concurrency` group is keyed on `publish-<ref>` with `cancel-in-progress: false` (publishing should never be cancelled mid-run). Files allowlist contract per `04-security-and-performance.md` §"Supply Chain" was already enforced in `package.json` (`files: ["dist", "configs", "fixtures", "README.md"]`) — TASK-043 added README; src/ and tests/ remain excluded. Unit tests at `tests/integration/publish-workflow.test.ts` (18 cases) parse the workflow as text (no YAML dep added) and assert: trigger shape (`release: published` + `workflow_dispatch` fallback), workflow-level permissions (`id-token: write`, `contents: read`, no `contents: write`), gate ordering (typecheck → build → test → publish, asserted via positional `String.search` indices), publish command flags (`--provenance`, `--access public`), token wiring (`NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}`), `setup-node` registry URL, and the version-verification step references `package.json` + `GITHUB_REF_NAME`. A dedicated `package.json` describe block guards the supply-chain contract: `files` includes `dist/configs/fixtures/README.md`, never `src`/`tests`/`.`/`*`, and the `typecheck`/`build`/`test` scripts the workflow invokes actually exist. Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (301/301, +18). The actual `npm publish --dry-run` end-to-end is gated behind a real maintainer release (requires `NPM_TOKEN` in GitHub Secrets and a published GitHub Release) — out of scope for the unit suite.
- [x] **TASK-043** — Package README with threshold rationale + research citations. New `README.md` at the repo root (already in `package.json` `files` allowlist alongside `dist`, `configs`, `fixtures`) acts as the npm landing page. Sections: hero (5 metrics + agentic-loop framing), tier table (fast vs. deep with latency budgets and trigger points), installation (incl. ts-morph optional peer), quick start (4 paths: copy preset, Claude Code `PostToolUse`, lint-staged pre-commit, ESLint v9 flat config) with concrete config snippets pulled from `configs/oxlint.fast.json` / `configs/oxlint.deep.json` and `fixtures/`, per-rule reference (every rule has tier, default options, computation summary, sample diagnostic, and a **Rationale** subsection citing the empirical work behind the default threshold), threshold-tuning cheatsheet (low-risk / refactor candidate / critical bands for all five metrics), architecture diagram (`createOnce` mandatory, fast tier never imports ts-morph), structured `data` payload reference for every diagnostic (so consumers know what to read programmatically), out-of-scope section explaining why auto-fix / readability / process metrics aren't here, References section with 12 academic citations: Chidamber & Kemerer (1994), Halstead (1977), McCabe (1976), Basili et al. (1996), Briand et al. (2000), Subramanyam & Krishnan (2003), Cartwright & Shepperd (2000), Lorenz & Kidd (1994), Lanza & Marinescu (2006), Radjenović et al. (EMSE 2013), Etzkorn et al. (2004), Aman et al. (2014), Jimenez et al. (Defects4J 2024). Validated: `tsc --noEmit` ✅, `npm run build` ✅ (ESM 31.87 KB / CJS 33.03 KB / dts 21.54 KB), `npm test` ✅ (283/283).

## Notes / Discoveries

- `vitest.config.ts` uses `passWithNoTests: true` so `vitest run` exits 0 before any tests exist (satisfies TASK-002 AC).
- `tsconfig.json` excludes `tests/` from compilation (test files compiled by Vitest's own pipeline). Once Lane D ships, may need a `tsconfig.test.json` for stricter type-checking.
- `node_modules`, `dist`, `package-lock.json` to be added/committed appropriately (lock file checked in; `node_modules`, `dist` ignored).
- TASK-001: `ts-morph` added as `devDependency` so `import('ts-morph').Project` in `ProjectSingleton` type-checks. It remains an *optional* runtime peer dependency (deep-tier rules only). `ReportDescriptor` was added to the contracts (not in the original architecture doc) to give `RuleContext.report()` a typed argument matching the OXLint/ESLint diagnostic shape.
- TASK-011 follow-up for TASK-014: hand-tracing `tests/fixtures/halstead/complex-fn.ts` against the COUNTING_MODEL.md classification yields `N₁=41, N₂=51` (vs. the fixture comment's `N₁=43, N₂=52`). The distinct counts (`η₁=19`, `η₂=30`) and the qualitative outcome (Effort ≫ 400) are unaffected, so the rule still flags `processPayment`. When TASK-014 wires the rule against the fixture, reconcile by either (a) re-running `computeHalstead` on the parsed file and updating the fixture comment, or (b) refining the COUNTING_MODEL if extra tokens (e.g., CallExpression `()`) are intended.
- TASK-041 surfaced a latent ESLint v10 incompatibility in CBO/DIT: `context.getFilename()` was deprecated in v9 and removed in v10. The existing rule unit tests passed because they use hand-rolled mock contexts that provide `getFilename` directly; only running the rules through real ESLint exposed the issue. Fix landed inline (TASK-041): `RuleContext.filename` is now the preferred read, with `getFilename?.()` as a legacy fallback. Going forward, deep-tier-style rules that need a filename should follow the same `filename ?? getFilename?.()` pattern.

## Blocked

(none)
